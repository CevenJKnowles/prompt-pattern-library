# Contributing

## Core rules
- **Do not rename** folders/files after publish (URLs + backlinks).
- One atomic change per commit where practical.
- Keep patterns atomic: one mechanism, one intent.

## Editing YAML
- Update `updated:` whenever content changes.
- Never reuse an `id:`.
