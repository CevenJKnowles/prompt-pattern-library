# Prompt Pattern Library

Canonical prompt-pattern taxonomy + an atomic pattern library.

## What this repo is
- **Taxonomy v1.0**: Domains → Categories → Subcategories (locked)
- **Phase 2**: One file per pattern (subcategory), with a consistent YAML+Markdown schema.

## Authoring
Open `docs/` as a Zettlr project root.

## Publishing (optional)
This repo includes `mkdocs.yml` for a clean static wiki build.

## Start here
- `docs/index.md`
- `docs/patterns/_index.md`
